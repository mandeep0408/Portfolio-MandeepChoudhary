# Public Assets

Place your profile photo here as `profile.jpg` (or update the path in `components/Hero.tsx`).

Recommended specifications:
- Format: JPG or PNG
- Size: At least 400x400px (square)
- File size: Optimized (under 200KB recommended)

The image will be automatically optimized by Next.js Image component.

